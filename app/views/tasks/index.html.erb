<h1>タスク一覧</h1>

<ur>
    
    <% @tasks.each do |task| %>
        <li><%= link_to task.id, task %>：<%= task.content %>：<%= task.status %></li>
    <% end %>
    
</ur>

<%= link_to 'new post' , new_task_path %>